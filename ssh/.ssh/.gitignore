# Ignore all private keys (no extension)
id_rsa
id_ed25519
id_ecdsa

# Ignore any files ending in .pem or .key
*.pem
*.key

# Ignore public keys and known_hosts (machine-specific, regenerated from private key)
*.pub
known_hosts

# But DO NOT ignore the config
!config

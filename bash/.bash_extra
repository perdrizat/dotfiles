# Fallback to vi if vim is not installed
vim() {
    if command -v vim >/dev/null 2>&1; then
        command vim "$@"
    else
        command vi "$@"
    fi
}
# set default editor
if command -v vim >/dev/null 2>&1; then
    export EDITOR='vim'
else
    export EDITOR='vi'
fi
export VISUAL="$EDITOR"
